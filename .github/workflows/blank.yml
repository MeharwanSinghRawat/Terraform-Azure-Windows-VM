name: CI
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
jobs:
  build:
    runs-on: MeharGit
      steps:
      -  name: Run a one-line script
         run: echo Hello, world!

